# -*- mode: snippet -*-
# name: int
# key: int
# condition: t
# --
\int${1:$(when (> (length yas-text) 0) "_{")
}${1:low}${1:$(when (> (length yas-text) 0) "}")
}${2:$(when (> (length yas-text) 0) "^{")
}${2:hi}${2:$(when (> (length yas-text) 0) "}")} `%`$0 \; \mathrm{d} ${3:x}
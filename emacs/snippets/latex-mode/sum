# -*- mode: snippet -*-
# name: sum
# key: sum
# condition: t
# --
\sum${1:$(when (> (length yas-text) 0) "_{")
}${1:i = 1}${1:$(when (> (length yas-text) 0) "}")
}${2:$(when (> (length yas-text) 0) "^{")
}${2:n}${2:$(when (> (length yas-text) 0) "}")} `%`$0